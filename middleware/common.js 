const isAdminMiddle = (req, res, next) => {
  const { isAdmin } = req;
  if (isAdmin) {
    next(); // если админ есть, то двигаемся дальше 
  } else {
    res.redirect('/')
  }
}
};


module.exports = isAdminMiddle; 
